Standing up an ELK Stack running on an EC2 instance.

We could use the AWS Elasticsearch services, however to have greater control over configuration of the elk stack,
we'll set up our own single node elk server.


